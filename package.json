{
  "name": "website-thanh-cong",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.3",
  "scripts": {
    "dev": "pnpm --parallel --filter client --filter server dev",
    "build": "pnpm --filter shared build && pnpm --filter client build && pnpm --filter server build && pnpm --filter sync-tool build",
    "check": "pnpm -r check",
    "check:env": "pnpm --filter sync-tool check:env",
    "test": "pnpm --filter shared test && pnpm --filter sync-tool test && pnpm --filter client test && pnpm --filter server test",
    "verify": "pnpm check && pnpm test && pnpm build",
    "lint": "pnpm -r lint"
  }
}
